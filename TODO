Create man page with sphinx (base on neversearch wiki)

Implement:
* Coloured output?
* --exclude option to exclude paths? Or should people use find?
  [IMHO; no. People can just use find + grep and pass via stdin]
